---
layout: "base.njk"
---

<div class="game">
  <h2 class="game__heading">{{ title }}</h2>
  <div class="game__image-wrapper">
      {# <img class="game__image" src="{{ image }}" alt="{{ image_alt }}"> #}
      {% EleventyImage image, image_alt, "(min-width: 30em) 50vw, 100vw" %}
  </div>
  <div class="game__content prose">
    {{ content | safe }}
  </div>
</div>